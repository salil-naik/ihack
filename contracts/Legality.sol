pragma solidity ^0.5.0;
import "./Mentor.sol";
import "./project.sol";
contract Legality {
    //check if student accpets condtion of income sharing
    require((mapping(student>investor)), pay(msg.sender));
    
}   